import { Auth } from '@layouts/index';
import { Authorization } from '@view/index';

const AuthorizationPage = () => {
	return (
		<Auth>
			<Authorization />
		</Auth>
	);
};

export default AuthorizationPage;
